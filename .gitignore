node_modules
dist
output.drawio
diagram.svg
C4*.puml
coverage
build

# gallery: downloaded tool + root-owned drawio export scratch (artifacts committed are docs/gallery/img + drawio + README)
docs/gallery*/plantuml.jar
docs/gallery/drawio/export/

# Claude Code harness state (session-local; CLAUDE.md lives at the repo
# root, NOT under .claude/, so it is unaffected). scheduled_tasks.lock
# is a runtime lock and settings.local.json is per-machine — not source.
.claude/

# Editor-specific (do not track)
.vscode/
.idea/
*.swp
